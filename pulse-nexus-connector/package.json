{
  "name": "pulse-nexus-connector",
  "version": "0.1.0",
  "description": "Cloudflare Worker that exposes the user's Pulse Nexus data over HTTPS so a ChatGPT Custom GPT (or any AI with Actions) can pull it on demand.",
  "author": "Faith Based Innovations",
  "private": true,
  "scripts": {
    "deploy": "wrangler deploy",
    "dev": "wrangler dev",
    "tail": "wrangler tail",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240725.0",
    "typescript": "^5.4.0",
    "wrangler": "^3.78.0"
  }
}
